USER INSTRUCTIONS:

Run the Main by passing the file name as a parameter e.g. java Main “logo” , will create a solution for the logo file. The input .in files are in the res directory. It is also in that directory that the solutions are saved.